<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".SearchActivity">

    <EditText
        android:id="@+id/edittext_artist_search"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:singleLine="true"
        android:imeOptions="actionSearch"
        android:hint="@string/search_hint"
        android:layout_toStartOf="@+id/imagebutton_clear_artist_field"
        android:layout_toLeftOf="@+id/imagebutton_clear_artist_field" />

    <ImageButton
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:id="@+id/imagebutton_clear_artist_field"
        android:layout_marginTop="5dp"
        android:layout_width="@dimen/button_clear_width"
        android:layout_height="wrap_content"
        android:background="@android:drawable/ic_menu_close_clear_cancel"
        android:onClick="clearArtistSearchButtonClick"
        android:contentDescription="@string/button_clear_description" />

    <view
        android:id="@+id/recyclerview_artist_results"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        class="android.support.v7.widget.RecyclerView"
        android:layout_below="@id/edittext_artist_search" />

</RelativeLayout>
